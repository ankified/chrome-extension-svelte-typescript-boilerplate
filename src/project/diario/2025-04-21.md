# Diário de Desenvolvimento - 2025-04-21

### 12:09:37
- Refatorado o controle de visibilidade das colunas 'type', 'scheduledDate' e 'status' na tabela de itens salvos para usar `$derived` a partir de `typeFilter`, eliminando loops infinitos e tornando o código mais idiomático para Svelte 5.
- Corrigido o cálculo e a renderização das pills de status ('Atrasado', 'Pendente', 'Agendado') na coluna 'status': agora o valor é calculado via `accessorFn` e renderizado diretamente no template Svelte, garantindo que `cell.getValue()` sempre retorne o valor correto.
- Eliminados erros de linter relacionados ao tipo de `columnVisibility` e ao uso de funções em vez de objetos no TanStack Table.
- Ajustado o callback `onColumnVisibilityChange` para usar um estado temporário apenas para satisfazer o tipo esperado pelo TanStack Table, sem afetar a reatividade principal.
- Arquivos modificados: `src/options/views/table/DataTable.svelte`, `src/options/views/table/columns.ts`.
- Problemas encontrados: loop infinito causado por $effect + $state, pills de status não sendo exibidas devido à ausência de accessorFn, erros de tipagem TS2322/2345 no controle de visibilidade.
- Todos os problemas foram resolvidos e a tabela está funcional e sem erros de linter.
- Próximos passos planejados:
   - Prosseguir com a implementação de ações em lote e paginação na tabela.
   - Refinar filtros por grupo, tag e data.
   - Melhorar a experiência visual e responsiva da tabela.

### 12:29:46
- Corrigido o botão de expandir/recolher todos na tabela de itens salvos (`ExpandAllButton.svelte`): agora utiliza `$derived` para garantir reatividade ao estado de expansão da tabela, permitindo expandir e colapsar todas as linhas corretamente.
- Corrigido erro de linter (TS7006) adicionando tipagem explícita `(row: any)` no map ao gerar o objeto de expansão.
- Arquivo modificado: `src/options/views/table/ExpandAllButton.svelte`.
- Problema encontrado: o botão só expandia, não colapsava, devido à falta de reatividade no estado `expanded`. O erro de linter surgia por falta de tipagem no parâmetro do map.
- Ambos os problemas foram resolvidos.
- Próximos passos planejados:
   - Prosseguir com as próximas melhorias da tabela: filtros, ações em lote, visualização rápida em Dialog e paginação. 