# Diário de Desenvolvimento - 2025-04-26

### 15:19:34
- **Correção:** Resolvido loop infinito (`Maximum update depth exceeded`) no componente `BulkTagAssignDialog.svelte`.
- **Detalhes:** O loop ocorria ao abrir ou fechar o diálogo devido à forma como os `$effects` liam e escreviam na variável de estado `currentKnownTags`. A solução envolveu refatorar os efeitos para separar a sincronização com a store global (`$tagsStore`) da limpeza do estado local ao fechar o diálogo, e ajustar a função `handleCreateAndAddTag` para adicionar novas tags diretamente ao estado local.
- **Arquivos modificados:** `src/options/views/table/BulkTagAssignDialog.svelte`.
- **Plano relacionado:** Corrige o problema anotado no final de `src/project/planos/plano-acoes-lote-tabela.md`.
- **Próximos passos planejados:**
    - Verificar e corrigir outros possíveis bugs nas funcionalidades de ações em lote da tabela.
    - Continuar a implementação das funcionalidades pendentes da tabela (ex: edição de data agendada, agrupamento por domínio, paginação). 