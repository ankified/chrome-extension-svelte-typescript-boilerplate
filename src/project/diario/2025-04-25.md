# Diário de Desenvolvimento - 2025-04-25

### 17:33:21
- Implementada a funcionalidade de ações em lote para Grupos e Tags na tabela de itens.
- Criados os componentes de diálogo `BulkGroupAssignDialog.svelte` e `BulkTagAssignDialog.svelte`.
- Implementada a lógica para adicionar/remover grupos e tags em múltiplos itens selecionados, incluindo sincronização entre listas de adição e remoção.
- Utilizado `bits-ui/checkbox` para seleção, com estilo `shadcn-svelte` aplicado via `cn()`.
- Refatorado o uso do `Checkbox` para renderização condicional do ícone `Check` (`{#if ...includes(...) }`) em vez de `Checkbox.Indicator`, após testes e ajustes.
- Corrigidos erros de linter (uso de `$$props` em runes, importações).
- Arquivos modificados/criados:
  - `src/options/views/table/BulkGroupAssignDialog.svelte`
  - `src/options/views/table/BulkTagAssignDialog.svelte`
  - (Potencialmente outros arquivos relacionados à tabela ou estado, se aplicável - revisar histórico)
- Próximos passos planejados: Integração final com a tabela, testes adicionais. 