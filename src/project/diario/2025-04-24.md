# Diário de Desenvolvimento - 2025-04-24

### 14:49:51
- **Visualização em Tabela (Itens Salvos):**
    - Substituídos os inputs de filtro das colunas "Notas" e "Flashcards" por botões de filtro (funcionalidade pendente), conforme item do plano (`src/options/views/table/DataTable.svelte`).
    - Implementada ordenação, filtragem e capacidade de agrupamento para as colunas "Agendado para" (`scheduledDate`) e "Status" (`src/options/views/table/columns.ts`).
    - Adicionados controles de filtro na UI para "Agendado para" (RangeCalendar) e "Status" (DropdownMenu com CheckboxItems) (`src/options/views/table/DataTable.svelte`).
    - Corrigidos erros de linter relacionados a tipos (`columns.ts`, `DataTable.svelte`).
- **Arquivos modificados:** `src/options/views/table/DataTable.svelte`, `src/options/views/table/columns.ts`.
- **Próximos passos planejados:**
    - Continuar implementando itens pendentes da visualização em tabela (ex: paginação, ações em lote, funcionalidade dos botões de filtro/data/status).

### 15:02:14
- **Visualização em Tabela (Itens Salvos):**
    - Corrigido problema onde o `Popover` do `RangeCalendar` da coluna "Agendado p/" fechava instantaneamente ao ser aberto.
    - Removido `$effect` redundante que forçava o fechamento do popover em `DataTable.svelte`.
- **Arquivos modificados:** `src/options/views/table/DataTable.svelte`.
- **Próximos passos planejados:**
    - Implementar a funcionalidade dos botões de filtro/data/status nas colunas da tabela.

### 16:58:36
- **Visualização em Tabela (Itens Salvos) - Filtro de Status:**
    - Atualizada a lógica de status em `columns.ts` para usar apenas 'Hoje', 'Pendente', 'Atrasado', 'Concluído'.
    - Modificado o controle de filtro de Status em `DataTable.svelte` de `CheckboxItem` para `RadioGroup` (seleção única).
    - Corrigido erro `props_invalid_value` ao usar `bind:value` com `undefined`, passando a usar `''` para estado sem filtro (`DataTable.svelte`).
    - Refatorada a aplicação do filtro de Status para usar `$effect`, resolvendo problema de reatividade onde o valor anterior era aplicado (`DataTable.svelte`).
- **Arquivos modificados:** `src/options/views/table/columns.ts`, `src/options/views/table/DataTable.svelte`.
- **Próximos passos planejados:**
    - Adicionar campo `completed` ao tipo `SavedItem` e lógica correspondente em `columns.ts` para o status 'Concluído'.
    - Implementar renderização visual (ex: Badges) para os status na célula da tabela.

### 17:11:23
- **Visualização em Tabela (Itens Salvos) - Marcar como Concluído:**
    - Adicionado campo `completed?: boolean` à interface `SavedItem` (`types.ts`).
    - Criados componentes `MarkAsCompleteDialog.svelte` e `StatusCellButton.svelte`.
    - Atualizada coluna `status` em `columns.ts` para usar `StatusCellButton` e incluir lógica para o estado "Concluído".
    - Corrigidos erros de linter:
        - Tipo explícito para prop `onConfirm` em `MarkAsCompleteDialog.svelte`.
        - Acesso incorreto a `row.original` dentro de `accessorFn` em `columns.ts`.
        - Uso de `updateItem` (não existente) comentado em `StatusCellButton.svelte` (funcionalidade pendente).
- **Arquivos modificados:** `src/types.ts`, `src/options/views/table/columns.ts`, `src/options/views/table/MarkAsCompleteDialog.svelte`, `src/options/views/table/StatusCellButton.svelte`.
- **Próximos passos planejados:**
    - Implementar/corrigir função `updateItem` em `storage.ts`.
    - Descomentar uso de `updateItem` em `StatusCellButton.svelte`.

### 17:29:35
- **Visualização em Tabela (Itens Salvos) - Correção Filtro de Status:**
    - Corrigido problema de reatividade onde o valor anterior do filtro de status era aplicado.
    - Removida a chamada `setFilterValue` do `onSelect` e adicionado um `$effect` para observar `statusFilter` e aplicar o filtro corretamente em `DataTable.svelte`.
- **Arquivos modificados:** `src/options/views/table/DataTable.svelte`.
- **Próximos passos planejados:**
    - Implementar renderização visual (ex: Badges) para os status na célula da tabela.

### 18:10:51
- **Correção**: Reatividade do status na visualização em tabela.
- **Detalhes**: Corrigido problema onde a UI da coluna "Status" na tabela não atualizava após marcar um item como concluído no diálogo. Refatorado `StatusCellButton.svelte` para derivar o `item` da store `savedItems` usando `itemId` passado como prop, em vez de receber o objeto `item` diretamente. Atualizada a definição da coluna "Status" em `columns.ts` para passar `itemId`.
- **Arquivos Modificados**:
  - `src/options/views/table/StatusCellButton.svelte`
  - `src/options/views/table/columns.ts`
- **Problema Resolvido**: UI do status não refletia a atualização no storage após confirmação no diálogo.
- **Próximos Passos**: Continuar implementação das melhorias de UX ou outras tarefas pendentes da tabela. 