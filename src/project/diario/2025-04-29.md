### 16:55:06
- Implementado o menu de opções gerais para a tabela de itens salvos (`DataTable.svelte`), concluindo o plano `plano-menu-opcoes-tabela.md`.
- Funcionalidades adicionadas:
  - Seleção de visibilidade de colunas via submenu.
  - Opção para remover todos os itens (com confirmação).
  - Links para abrir diálogos de gerenciamento global de grupos e tags.
- Refatorações realizadas:
  - Criado store `uiState` para gerenciar visibilidade de diálogos globais.
  - `SavedItemsView` atualizado para usar `uiState`.
  - `ManageGroupsDialog` refatorado para operação global.
  - `columns.ts` modificado para passar instância da tabela ao botão do menu.
  - `DropdownMenuHeaderButton` implementado com submenu e lógica de visibilidade.
  - `storage.ts` atualizado com a função `deleteAllItems`.
- Correções:
  - Resolvidos loops infinitos em `DataTable` relacionados à reatividade `$effect` e `columnVisibilityState`.
  - Corrigido o disparo do filtro de tipo (`typeFilter`) que estava ausente.
  - Resolvida a interação entre a visibilidade automática de colunas (baseada no `typeFilter`) e a seleção manual no menu.
  - Corrigido erro de linter em `SavedItemsView` (componentes não auto-fechados).
  - Corrigidos erros de linter em `DropdownMenuHeaderButton` (reatribuição de estado exportado) usando objeto `$state`.
- Arquivos modificados/criados: `src/lib/stores/uiState.svelte.ts` (criado), `src/options/views/SavedItemsView.svelte`, `src/options/components/ManageGroupsDialog.svelte`, `src/options/views/table/columns.ts`, `src/options/views/table/DropdownMenuHeaderButton.svelte`, `src/options/views/table/DataTable.svelte`, `src/storage.ts`, `src/project/planos/plano-menu-opcoes-tabela.md`.
- Próximos passos planejados: Iniciar a próxima tarefa prioritária do projeto. 