# Diário de Desenvolvimento - 2025-04-06

### 15:15:00
- Refinamento do indicador "Ler Mais Tarde" (agendamento) nos cards de itens salvos (`SavedItemsCardView.svelte`).
- O ícone `<CalendarClock>` agora aparece apenas para itens agendados, posicionado à direita, próximo às Ações.
- O ícone `<CalendarClock>` é agora um botão que chama a função (placeholder) `editSchedule`.
- O tooltip do ícone `<CalendarClock>` exibe a data/hora formatada de forma relativa e detalhada (ex: "Hoje, às 15:30h", "Amanhã, às 09:00h", "Em 5 dias, às 10:00h") usando a nova função `formatScheduleTooltip`.
- O ícone `<Info>` (data de adição) só é exibido para itens *não* agendados, mantendo sua posição à esquerda.
- Foram importadas e utilizadas funções adicionais de `date-fns` (`isToday`, `isTomorrow`, `isYesterday`, `differenceInDays`, `format`).
- **Arquivos modificados:** `src/options/views/card/SavedItemsCardView.svelte`.
- **Tarefa concluída:** Conclui o item "Melhorias de Experiência do Usuário - 14. Refinamento do Indicador \"Ler Mais Tarde\" nos Cards" do arquivo `src/project/plano-acoes-prioritarias.md`.
- **Próximos passos planejados:** Implementar a funcionalidade real de edição do agendamento na função `editSchedule`.

### 15:25:25
- **Correção:** Ajustada a lógica de exibição do ícone de informação (`<Info>`) nos cards de itens salvos (`SavedItemsCardView.svelte`) para garantir que ele seja sempre visível, mesmo quando o item está agendado.
- A implementação anterior removia incorretamente o ícone `<Info>` para itens agendados.
- **Arquivos modificados:** `src/options/views/card/SavedItemsCardView.svelte`.
- **Observação:** Esta correção complementa a implementação da tarefa "Melhorias de Experiência do Usuário - 14. Refinamento do Indicador \"Ler Mais Tarde\" nos Cards".

### 15:55:19
- **Implementação:** Adicionados botões/contadores para Notas e Flashcards no rodapé dos cards em `SavedItemsCardView.svelte`.
    - A contagem é obtida diretamente de `item.noteIds.length` e `item.flashcardIds.length`.
    - Foram adicionadas funções placeholder (`viewNotes`, `viewFlashcards`) para interação futura.
    - O componente pai (`SavedItemsView.svelte`) foi modificado para remover a lógica de contagem explícita e ajustar a ordenação para usar `.length`.
- **Correção:** Resolvidos erros de lint em `SavedItemsCardView.svelte` movendo as declarações `{@const noteCount}` e `{@const flashcardCount}` para serem filhas imediatas do bloco `#each`, conforme exigido pelo Svelte.
- **Arquivos modificados:** `src/options/views/SavedItemsView.svelte`, `src/options/views/card/SavedItemsCardView.svelte`.
- **Próximos passos planejados:** 
    - Implementar a funcionalidade real de clique nos botões de Notas e Flashcards (abrir diálogo/navegar para visualização específica).
    - Implementar a funcionalidade de edição de agendamento (`editSchedule`).

### 16:56:46
- **Modificação UI (Itens Salvos):**
    - Removido o botão global "Remover Todas as Tags" de `SavedItemsView.svelte`.
    - Restaurado o botão/dropdown "Gerenciar Tags" ao lado do botão "Gerenciar Grupos" (que substituiu o botão de remover tags anteriormente).
- **Modificação UI (Cards):**
    - Implementado scroll horizontal (`overflow-x-auto`) para as seções de Grupos e Tags em `SavedItemsCardView.svelte` para lidar com excesso de pills.
    - Implementado `ScrollArea` do Shadcn-Svelte para a seção de Comentários em `SavedItemsCardView.svelte`.
- **Correção Lint:**
    - Resolvidos erros de lint em `SavedItemsView.svelte` relacionados a comentários (`{/* */}` -> `<!-- -->`) e ao uso de `let:builder` com `asChild` no `DropdownMenu.Trigger` (removido `let:builder`).
- **Arquivos modificados:** `src/options/views/SavedItemsView.svelte`, `src/options/views/card/SavedItemsCardView.svelte`.
- **Próximos passos planejados:** 
    - Refinar visualmente o scroll horizontal das tags/grupos se necessário (ex: adicionar botões de navegação).
    - Implementar a funcionalidade real de Gerenciamento de Grupos. 