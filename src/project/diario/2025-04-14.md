# Diário de Desenvolvimento - 2025-04-14 

### 18:08:03
- **Melhoria UI (Ordenação):** Implementada exclusão mútua e prevenção de repetição para critérios de ordenação no `FilterSheet.svelte`.
- **Implementação:**
  - Adicionada lógica à renderização dos `<Select.Item>` de critérios para desabilitá-los (`disabled={...}`) se:
    - Um critério mutuamente exclusivo (Data Adição/Agendada ou Título/URL) já estiver selecionado em outro nível.
    - O próprio critério já estiver selecionado em outro nível.
  - Corrigido erro de lint adicionando tipos explícitos aos parâmetros de callback em `.filter` e `.some`.
  - Modificada a função `addSortLevel` para que novos níveis de ordenação sejam adicionados com `criterion: ''` (vazio) em vez de um padrão, exibindo o placeholder "Critério...".
- **Arquivos modificados:**
  - `src/options/components/FilterSheet.svelte`
- **Próximos passos planejados:**
  - Implementar agrupamento visual por data na visualização de cards quando o critério de ordenação primário for baseado em data.
  - Definir estratégia de agrupamento (por dia, semana, mês, etc.). 