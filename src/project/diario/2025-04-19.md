# Diário de Desenvolvimento - 2025-04-19 

### 11:47:43
- Corrigida a definição da coluna "type" na tabela de itens salvos para garantir que o valor retornado por `cell.getValue()` seja sempre o objeto `{ isReadLater, typeLabel }` esperado pelo template em `DataTable.svelte`.
- Agora, o ícone e o label são exibidos corretamente na coluna "Tipo", e o hífen só aparece quando realmente não houver valor definido.
- Arquivo modificado: `src/options/views/table/columns.ts`.
- Problema resolvido: O template da coluna "Tipo" exibia sempre um hífen, mesmo quando havia valores válidos, devido à ausência de um `accessorFn` adequado na definição da coluna. A solução foi implementar o `accessorFn` para garantir o valor correto.
- Próximos passos planejados:
   - Testar a renderização da tabela com diferentes tipos de itens (Bookmark e Ler Mais Tarde).
   - Validar se a ordenação e filtragem por tipo continuam funcionando corretamente.
   - Revisar outras colunas para garantir que todas utilizam corretamente o padrão de acesso aos dados. 